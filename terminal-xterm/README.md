# timmfin terminal demo

A small xterm.js setup for the "terminal mode" concept. Installs xterm via npm and serves a ready-to-run prototype with a few lines of animated copy.

## Setup

```bash
npm install
npm run dev
```

Then open <http://localhost:5173>.
